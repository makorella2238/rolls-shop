
{
  "env": {
    "URL_MONGODB": "YOUR_MONGODB_URL",
    "PORT": "YOUR_PORT"
  },

  "version": 2,
  "builds": [
    {
      "src": "./index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}